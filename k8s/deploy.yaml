apiVersion: apps/v1
kind: Deployment
metadata:
  name: md-anexo-omie-service
  labels:
    app: md-anexo-omie-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: md-anexo-omie-service
  template:
    metadata:
      labels:
        app: md-anexo-omie-service
    spec:
      containers:
      - name: md-anexo-omie-service
        image: registry.digitalocean.com/oondemand/md-anexo-omie-service:start
        ports:
        - containerPort: 4003
        envFrom:
        - secretRef:
            name: vitta-env-secret